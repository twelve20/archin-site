# 🚀 Быстрый деплой на VPS

## Что вам понадобится

1. ✅ IP адрес вашего VPS
2. ✅ SSH доступ (логин/пароль или SSH ключ)
3. ✅ Домен (например: mysite.com)
4. ⏱️ 15-20 минут времени

---

## 📋 Пошаговая инструкция

### ШАГ 1: Настройка DNS (5 минут)

Зайдите в панель управления вашего доменного регистратора и добавьте **A-записи**:

```
Тип    Имя    Значение           TTL
A      @      ВАШ_IP_АДРЕС_VPS   3600
A      www    ВАШ_IP_АДРЕС_VPS   3600
```

💡 Проверить DNS можно командой: `nslookup your-domain.com`

---

### ШАГ 2: Подключение к серверу

```bash
ssh root@ВАШ_IP_АДРЕС_VPS
```

Или если у вас другой пользователь:
```bash
ssh username@ВАШ_IP_АДРЕС_VPS
```

---

### ШАГ 3: Установка окружения (5 минут)

Скопируйте скрипт установки на сервер:

```bash
# На ВАШЕМ компьютере (не на сервере):
cd c:\Users\777\Documents\GitHub\archin-site
scp deploy/setup-server.sh root@ВАШ_IP:~/
```

Затем на сервере запустите:

```bash
# На СЕРВЕРЕ:
bash setup-server.sh your-domain.com
```

✅ Скрипт автоматически установит: Node.js, PM2, Nginx, Certbot

---

### ШАГ 4: Загрузка кода (2 минуты)

```bash
# На СЕРВЕРЕ:
cd /var/www/archin-site
git clone https://github.com/twelve20/archin-site.git .
```

---

### ШАГ 5: Деплой приложения (5 минут)

```bash
# На СЕРВЕРЕ:
bash deploy/deploy-app.sh
```

✅ Ваш сайт теперь работает на http://your-domain.com

---

### ШАГ 6: Получение SSL сертификата (2 минуты)

```bash
# На СЕРВЕРЕ:
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

Certbot спросит ваш email и согласие с условиями. Ответьте на вопросы.

✅ Ваш сайт теперь доступен с HTTPS: **https://your-domain.com** 🎉

---

## 🔄 Как обновить сайт в будущем

### Вариант А: Быстрое обновление с сервера

```bash
# На СЕРВЕРЕ:
cd /var/www/archin-site
git pull origin main
bash deploy/update-app.sh
```

### Вариант Б: Автоматический деплой с вашего компьютера

1. Отредактируйте файл `deploy/local-deploy.sh`:
   - Замените `YOUR_SERVER_IP` на IP вашего VPS
   - Замените `your-domain.com` на ваш домен
   - Замените `root` на вашего SSH пользователя

2. Запустите:
```bash
cd c:\Users\777\Documents\GitHub\archin-site
bash deploy/local-deploy.sh
```

---

## 🛠️ Полезные команды

### Проверка статуса
```bash
pm2 status          # Статус приложения
pm2 logs archin-site # Просмотр логов
```

### Управление приложением
```bash
pm2 restart archin-site  # Перезапуск
pm2 stop archin-site     # Остановка
pm2 start archin-site    # Запуск
```

### Nginx
```bash
sudo systemctl status nginx   # Статус Nginx
sudo nginx -t                 # Проверка конфигурации
sudo systemctl restart nginx  # Перезапуск Nginx
```

---

## ❓ Проблемы?

### Сайт не открывается

1. **Проверьте DNS**: `nslookup your-domain.com` (должен вернуть IP вашего сервера)
2. **Проверьте приложение**: `pm2 status` и `pm2 logs archin-site`
3. **Проверьте Nginx**: `sudo systemctl status nginx`
4. **Проверьте firewall**: `sudo ufw status` (порты 80, 443 должны быть открыты)

### Приложение падает

```bash
pm2 logs archin-site  # Смотрите логи ошибок
```

### SSL сертификат не выдается

- Убедитесь что DNS записи корректны и распространились
- Порты 80 и 443 должны быть открыты
- Домен должен быть доступен по HTTP до получения сертификата

---

## 📞 Дополнительная помощь

Подробная документация в файле: [deploy/README.md](deploy/README.md)
